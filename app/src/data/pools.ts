/**
 * Dados de bolões — apenas dados reais.
 * Sem mocks: bolões vêm de localStorage ou serão criados pelos usuários.
 */

import type { Pool } from '@/types';

/** Lista vazia inicial — sem bolões de exemplo */
export const initialPools: Pool[] = [];
